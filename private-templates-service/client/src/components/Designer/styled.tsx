import styled from 'styled-components';

export const OuterDesignerWrapper = styled.main`
  display: flex;
  flex-direction: column;
  height: 100%;
  overscroll-behaviour: contain;
`;

export const DesignerWrapper = styled.div`
  height: 100%;
`;
